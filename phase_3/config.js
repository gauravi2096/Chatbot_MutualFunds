/**
 * API base URL for the Phase 2 backend.
 * - Local dev: "" (uses same origin when served by FastAPI)
 * - Vercel: Set API_BASE_URL in Vercel env; build script injects it here.
 */
window.API_BASE = "";
